import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Les assets statiques (images, fonts) sont servis depuis /public
  // Pas de config supplémentaire nécessaire pour ce projet
};

export default nextConfig;
